# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# OS
.DS_Store

# Aether
aether-redis-data/
# local-scope MCP config (machine-specific absolute paths) — keep out of history
.mcp.local.json
# operator-panel secret (token) — never commit; use .env.example as the template
.env
aether/.env
# TLS material for the cross-machine bus — never commit private keys
aether/certs/

# Python build artifacts
build/
dist/
*.egg-info/
