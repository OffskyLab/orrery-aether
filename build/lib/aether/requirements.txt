redis>=5.0,<6.0
PyYAML>=6.0
pytest>=8.0
# Stargazer (Phase 3) — read-only dashboard backend. Pinned to a coherent set
# (fastapi 0.115.x ↔ starlette 0.41.x ↔ sse-starlette 2.x) to avoid the
# fastapi/starlette 1.0 incompatibility.
fastapi>=0.115.6,<0.116
starlette>=0.40,<0.42
sse-starlette>=2.1,<3
uvicorn>=0.30
httpx>=0.27
