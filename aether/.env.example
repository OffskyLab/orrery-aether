# Copy to `.env` (gitignored) — read by docker-compose.yml for the operator panel.
# The operator panel is the PRIVILEGED write path; it refuses to start without a
# token (§18.3 localhost+token). Generate a strong one, e.g.:
#     openssl rand -hex 32
AETHER_OPERATOR_TOKEN=change-me
