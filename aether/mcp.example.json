{
  "_comment": "Register the Aether MCP server in a project's .mcp.json so Claude Code auto-discovers the aether_* tools. Use a DISTINCT --identity per project (NOT the project's Observatory id, to avoid inbox collisions). Paths must be absolute (Claude Code launches with the project as cwd). Requires Redis running.",

  "mcpServers": {
    "aether": {
      "command": "python3",
      "args": [
        "/Users/abnertsai/JiaBao/grady/orrery-aether/aether/mcp_server.py",
        "--identity", "event_storming_tool-mcp"
      ],
      "env": {
        "AETHER_REDIS_DB": "0"
      }
    }
  }
}
